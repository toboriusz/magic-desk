const state = {
	//States
}

const getters = {
	//Getters
}

const actions = {
	//Actions
}

const mutations = {
	//Mutations
}

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
}